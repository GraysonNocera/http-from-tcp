# HTTP from TCP

This project demonstrates how to implement a simple HTTP server using raw TCP sockets in Golang. It listens for incoming TCP connections, processes HTTP requests, and sends back HTTP responses.